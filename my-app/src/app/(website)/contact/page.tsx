import Scheduler from "@/app/(website)/components/scheduler/Scheduler";

export default function ContactPage() {
  return (

    <div>
    {/* Scheduler Section */}
    <Scheduler />
    </div>
  );
}
